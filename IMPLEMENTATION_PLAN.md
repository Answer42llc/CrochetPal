## 阶段 1: 梳理原子化输入链路
**目标**: 确认 round summary 在原子化请求中的缺口位置。
**成功标准**: 明确需要修改的数据模型、输入构造和测试点。
**测试**: 检查现有原子化请求测试与服务层输入构造逻辑。
**状态**: 已完成

## 阶段 2: 接入本圈 Summary
**目标**: 将当前圈的 summary 加入 `AtomizationRoundInput` 并传到原子化请求。
**成功标准**: `PatternImportService` 和 LLM 请求体都包含 round summary。
**测试**: 更新单元测试验证输入 JSON 与服务层传参。
**状态**: 已完成

## 阶段 3: 编译与回归验证
**目标**: 确认修改后测试和编译均通过。
**成功标准**: 相关测试通过，iOS 模拟器构建成功。
**测试**: 执行相关 `xcodebuild test` 和 `xcodebuild build`。
**状态**: 已完成
