## 阶段 1: 定位与复现
**目标**: 确认重复结构 round 在原文总针数与展开结果冲突时的失败路径，并补上可复现测试。
**成功标准**: 新测试能稳定复现当前 `atomization_target_stitch_count_mismatch` 场景。
**测试**: `xcodebuild test` 运行相关 `PatternImportServiceTests`
**状态**: 进行中

## 阶段 2: 目标针数协调
**目标**: 为重复结构这类可确定展开结果的 round 增加通用协调逻辑，避免被错误括号总针数阻塞。
**成功标准**: 遇到原文总针数与可确定展开结果冲突时，系统能产出可执行的 atomic actions，并得到一致的目标针数。
**测试**: 新增单元测试覆盖冲突输入与正常输入。
**状态**: 未开始

## 阶段 3: 回归验证
**目标**: 确认改动不会破坏现有 atomization、执行引擎与编译流程。
**成功标准**: 相关测试通过，项目成功编译。
**测试**: 运行相关测试并执行一次目标 scheme 的 build
**状态**: 未开始
